// DOM manipulation in JavaScript involves interacting with HTML elements on a web page.

// 1. Selecting Elements. (using methods like getElementById(), getElementsByClassName(), getElementsByTagName(), and querySelector().)

// Create new element
let newElement = document.createElement('div');

// Set attributes
newElement.id = 'newElement';
newElement.textContent = 'New element content';

// Append to parent element
parentElement.appendChild(newElement);

----

// Event handling
// Add event listener
elementById.addEventListener('click', function() {
    console.log('Element clicked');
});

